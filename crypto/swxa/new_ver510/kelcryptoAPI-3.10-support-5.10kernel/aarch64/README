使用方法：先到swcsm目录下（swcsm目录名不可以改变）  ./INSTALL swcsm36.drv
执行完后会产生文件swcsm36.ko 以及Module.symvers文件，在使用内核调用接口时会用到Module.symvers文件。

再回来 ./INSTALL sm.drv编译出sm.ko，本驱动模块是注册cryptoAPI接口
先使用insmod 命令加载swcsm36.ko，再用insmod命令加载sm.ko

